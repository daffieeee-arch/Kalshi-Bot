## What
-

## Verify
- [ ] `pytest -m "not integration"`
- [ ] Live demo check if this touches markets or auth (`pytest -m integration` or `discover-btc-15m`)

## Merge
Squash into `main`. GitHub deletes the head branch after merge.
